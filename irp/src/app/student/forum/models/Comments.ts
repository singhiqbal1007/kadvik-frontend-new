import { Time } from '@angular/common';

export class Comments{
    prn: number;
    commentText: string;
    postId:number;
    commentId:number;
    firstName:String;
    date:Date;
    time:Time;
}